[alias]
xtask = "run -p xtask --"

# Work around sporadic `rust-lld` crashes (SIGBUS) observed during large test
# links in constrained environments by forcing single-threaded linking.
[target.x86_64-unknown-linux-gnu]
rustflags = ["-C", "link-arg=-Wl,--threads=1"]
